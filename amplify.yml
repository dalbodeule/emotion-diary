version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22 && node --version
        - corepack enable && npx --yes npm install
    build:
      commands:
        - npm build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"